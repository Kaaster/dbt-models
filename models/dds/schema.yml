
version: 2

models:
    - name: dds__store__products
      columns:
          - name: product_id
            description: "The primary key for this table"
            tests:
                - unique
                - not_null
    - name: dds__store__warehouses
      columns:
          - name: warehouse_id
            description: "The primary key for this table"
            tests:
                - unique
                - not_null
